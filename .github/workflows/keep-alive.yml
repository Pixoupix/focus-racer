name: Keep Alive

# Disabled - no longer needed on dedicated server (was for Render free tier)
# on:
#   schedule:
#     - cron: '*/14 * * * *'

on:
  workflow_dispatch: # Manual trigger only

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Server
        run: curl -sf "https://focusracer.swipego.app/api/health" -o /dev/null || true
